---
import { Image } from "astro:assets";
import config from "@config/config.json";
import social from "@config/social";

const socialItems = social.networks;
---
<section class="bg-zinc-50 dark:bg-zinc-900">
  <div class="mx-auto max-w-screen-lg px-4 py-6 sm:px-6 lg:px-8 text-center">
    <h1 class="text-3xl sm:text-4xl md:text-5xl font-bold text-zinc-900 dark:text-white mb-4">
      {config.author.name}
    </h1>
    <p class="text-lg text-zinc-500 dark:text-zinc-400 mb-4">
      {config.author.bio}
    </p>
           
    </div>
  </div>
</section>






